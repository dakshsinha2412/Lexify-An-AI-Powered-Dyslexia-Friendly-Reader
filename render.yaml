services:
  - type: web
    name: lexify-app
    env: node
    plan: free
    buildCommand: npm run build
    startCommand: node server/index.js
    envVars:
      - key: NODE_VERSION
        value: 20.0.0
      - key: GEMINI_API_KEY
        sync: false
